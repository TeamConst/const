import Layout from "../components/layout";
import Buy from "../components/buy";

const Buysell = () => {
  return (
    <div>
      {/* 전체 css 이걸로 설정해 줄 것임 */}
      <Layout></Layout>
      <Buy></Buy>
      {/* <MintForm></MintForm> */}
    </div>
  );
};

export default Buysell;
